extends layout_admin

block content
  h1 Admin Login
  h2 Get Stats
  form(action="/admin/stats", method="post")
    label(for="password") Password: 
    input#admin-stats-login(type="text", name="password")
    button(type="submit") Submit